{
  "name": "package.json",
  "version": "1.0.0",
  "description": "",
  "default": "other.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "Mohammed Sanaan",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.8.2"
  }
}
